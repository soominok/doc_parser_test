# ─────────────────────────────────────────────
# PDF 파싱 라이브러리
# ─────────────────────────────────────────────
docling>=2.0.0              # 문서 구조 인식 (heading 계층, 표, 레이아웃)
pdfplumber>=0.11.0          # 정확한 텍스트/표 추출 (좌표 기반)
PyMuPDF>=1.24.0             # 폰트 크기·굵기 메타데이터 추출
pdfminer.six>=20221105      # pdfplumber 내부 의존성

# ─────────────────────────────────────────────
# LLM 연동 (모호한 구조 보완)
# ─────────────────────────────────────────────
openai>=1.30.0              # GPT-4o 비전 모델 활용 (선택)
anthropic>=0.30.0           # Claude API 활용 (선택)
python-dotenv>=1.0.0        # .env 파일에서 API Key 로드

# ─────────────────────────────────────────────
# 유틸리티
# ─────────────────────────────────────────────
Pillow>=10.0.0              # 페이지 이미지 렌더링 (LLM Vision 입력용)
pandas>=2.0.0               # 표 데이터 처리
tabulate>=0.9.0             # DataFrame → Markdown 표 변환
numpy>=1.24.0               # 폰트 크기 통계 분석
tqdm>=4.66.0                # 진행률 표시
loguru>=0.7.0               # 구조화된 로깅
