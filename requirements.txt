# PDF → Markdown 하이브리드 파서 - 의존 라이브러리
# 설치 명령: pip install -r requirements.txt

# ── 문서 구조 분석 (heading/표 감지) ─────────────────────────────
# Docling: IBM 오픈소스, AI 기반 PDF 구조 파악 (heading/표/리스트 분류)
docling>=2.0.0

# ── 폰트 분석 (heading 계층 구조 결정) ───────────────────────────
# PyMuPDF: PDF 렌더링 + 폰트 크기/굵기 추출 (빠르고 정확함)
pymupdf>=1.24.0

# ── 정확한 텍스트/표 추출 ────────────────────────────────────────
# pdfplumber: 문자 단위 정밀 추출, 표 경계선 감지
pdfplumber>=0.11.0

# ── 웹 UI ────────────────────────────────────────────────────────
# Gradio: 단일 파일로 웹 UI 구현, 파일 업로드/다운로드 내장
gradio>=4.0.0

# ── LLM 정제 (선택 사항) ─────────────────────────────────────────
# Anthropic: Claude API 클라이언트 (LLM 기반 후처리 활성화 시 필요)
anthropic>=0.30.0
