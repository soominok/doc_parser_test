# ============================================================
# 사내 문서 RAG 챗봇 - 의존성 패키지
# ============================================================
# 설치: pip install -r requirements.txt
# Python 3.10 이상 필요
# ============================================================

# ── PDF 파싱 ──────────────────────────────────────────────
docling>=2.31.0            # IBM Research의 AI 기반 PDF 구조 분석기 (핵심 파서)
PyMuPDF>=1.25.0            # PDF 텍스트/이미지 추출 + 폰트 메타데이터 (heading 분석용)

# ── LLM / Embedding ──────────────────────────────────────
openai>=1.60.0             # OpenAI API 클라이언트 (GPT-4o, Embedding, Vision)
langchain>=0.3.0           # LLM 오케스트레이션 프레임워크
langchain-openai>=0.3.0    # LangChain용 OpenAI 통합
langchain-core>=0.3.0      # LangChain 핵심 모듈
langchain-chroma>=0.2.0    # LangChain용 ChromaDB 통합

# ── 벡터 저장소 ──────────────────────────────────────────
chromadb>=0.6.0            # 로컬 벡터 데이터베이스

# ── 유틸리티 ─────────────────────────────────────────────
python-dotenv>=1.0.0       # .env 파일에서 환경변수 로드
